import PinnedItemPlaceholder from './pinned-item-placeholder'

describe('Widgets/GitHub/PinnedItemPlaceholder', () => {
  it('matches the snapshot', () => {
    expect(PinnedItemPlaceholder).toMatchSnapshot()
  })
})
